import { Component, Input } from '@angular/core';

@Component({
    selector: 'client-node',
    template: ``,
    styles: [``],
    styleUrls: ['./node.scss']
})
export class ClientNodeComponent {
    @Input() tree;
}
