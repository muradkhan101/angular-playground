import { Component, Input } from '@angular/core';

@Component({
    selector: 'building-node',
    template: ``,
    styles: [``],
    styleUrls: ['./node.scss']
})
export class BuildingNodeComponent {
    @Input() tree;
}
